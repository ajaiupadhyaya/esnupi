import { MacintoshDesktop } from "@/components/desktop/MacintoshDesktop";

/**
 * Homepage: full-screen Mac OS 8–style desktop; Hydra synth is the “wallpaper” (see SiteLayout).
 */
export default function Home() {
  return <MacintoshDesktop />;
}
